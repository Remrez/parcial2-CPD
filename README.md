Para ejecutar el código (Windows):
-Instalar Windows MPI (https://learn.microsoft.com/es-es/message-passing-interface/microsoft-mpi)
-Correr el siguiente comando dentro de la carpeta del repo  
pip install -r requirements.txt

-Correr python main.py
